# CRAFT Dependencies
# Install with: pip install -r requirements.txt
# Flash Attention installed separately (see end of file).

--extra-index-url https://download.pytorch.org/whl/cu128

# PyTorch stack (CUDA 12.8 wheels)
torch==2.9.1
torchvision==0.24.1
triton==3.5.1

# Transformers ecosystem
transformers==4.57.6
datasets>=2.16.0
accelerate>=0.26.0
peft>=0.8.0

# Training framework
ms-swift==4.1.3

# Inference
vllm==0.16.0
openai>=1.12.0
socksio>=1.0.0

# Distributed training
deepspeed>=0.14.0

# Configuration
pyyaml>=6.0
python-dotenv>=1.0.0

# Utilities
loguru>=0.7.0
tqdm>=4.65.0

# Flash Attention 2.8.3 (install AFTER torch is in place, requires CUDA toolchain)
# pip install flash-attn==2.8.3 --no-build-isolation
